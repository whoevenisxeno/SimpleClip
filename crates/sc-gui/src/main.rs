fn main() {
    // Phase 5 replaces this with the egui client (wizard, gallery, trim, tray).
    // It connects to scd over the same IPC as `sc`; the daemon never depends on it.
    eprintln!("sc-gui: not implemented until Phase 5");
    std::process::exit(1);
}
